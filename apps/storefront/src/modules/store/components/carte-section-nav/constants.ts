// Combined height of the fixed main nav (h-16, 64px) and this sticky
// section-nav bar (~56px): the offset every Carte section must reserve
// so an anchor jump never lands with its title hidden underneath both.
export const CARTE_NAV_OFFSET_PX = 120
