/dist
.env
.DS_Store
/uploads

# One-off snapshot produced by export-menu-data.ts / consumed by
# import-menu-data.ts — regenerated per run, never meant to be versioned.
/tmp/menu-snapshot.json
# Factures générées par le module File local (issue-invoice.ts) — un fichier
# par exécution du workflow, y compris chaque run de test HTTP (sa propre
# base jetable, donc son propre F-2026-000001). Ne concerne pas les autres
# assets de static/ (ex. samoussas.jpg), qui restent suivis.
static/private-*.pdf
/node_modules
yarn-error.log

.idea

coverage

!src/**

./tsconfig.tsbuildinfo
medusa-db.sql
build
.cache

.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

.medusa