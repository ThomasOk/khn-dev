STORE_CORS=http://localhost:8000,https://docs.medusajs.com
ADMIN_CORS=http://localhost:5173,http://localhost:9000,https://docs.medusajs.com
AUTH_CORS=http://localhost:5173,http://localhost:9000,https://docs.medusajs.com
REDIS_URL=redis://localhost:6379
JWT_SECRET=supersecret
COOKIE_SECRET=supersecret
DATABASE_URL=
DB_NAME=medusa-backend
# Base URL of the storefront app — used to build the cancellation link in the
# table-reservation confirmation email (ticket 06).
STOREFRONT_URL=http://localhost:8000
# Cloudflare R2 (S3-compatible) — file storage for product images, shared
# across local/staging/prod so the DB's image URLs stay valid everywhere.
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_REGION=auto
S3_BUCKET=
S3_ENDPOINT=
S3_FILE_URL=
